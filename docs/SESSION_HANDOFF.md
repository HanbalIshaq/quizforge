# QuizForge — Session Handoff Document

**Paste this whole document into the new Claude chat as your first message** and add your specific next ask at the end. Claude will then have full context to continue from where we left off.

> **How to keep this doc fresh:** at the end of every working session, tell Claude *"update SESSION_HANDOFF.md with what we did this session"* and commit the result.

---

## 1. Project: QuizForge

A self-hostable online quiz + exam + live poll + form-builder + assessment platform. Built to compete with Quizizz, ClassMarker, Kahoot, Mentimeter, JotForm. Currently at ~180 features shipped.

## 2. Where everything lives

| | |
|---|---|
| **Local code** | `C:\Users\Aflat\QuizForge` (Windows) |
| **GitHub repo** | <https://github.com/HanbalIshaq/quizforge> |
| **Production URL** | <https://quizforge-rgru.onrender.com> |
| **Host** | Render.com (free web service) |
| **Database** | Neon PostgreSQL (free tier, persistent) |
| **GitHub user** | `HanbalIshaq` |
| **Render service** | `quizforge` (auto-deploys from `main` branch) |

## 3. Credentials (DO NOT share publicly)

### Owner account (super admin on production)
- Email: `mr.hanbal@gmail.com`
- This is the first user; auto-promoted to super admin; gets Enterprise plan

### Neon Postgres connection string (already set on Render as `DATABASE_URL`)
```
postgresql://neondb_owner:npg_C4yMZoKHlp5J@ep-mute-thunder-aqsz4egb.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require
```

### Render env vars currently set
- `SECRET_KEY` (auto-generated by Render)
- `DATABASE_URL` (Neon string above)
- `PORT` (10000, set by render.yaml)

### Optional env vars (not yet set — features gracefully disabled until you set them)
- `ANTHROPIC_API_KEY` — enable AI quiz generator (also flip the feature flag on)
- `OPENAI_API_KEY` — alternative to Anthropic
- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM` — enable email for password resets
- `SHOW_RESET_LINK=1` — show the password-reset URL in a flash message when SMTP is not configured (good for dev)
- `REQUIRE_APPROVAL=true` — gate new signups behind admin approval

## 4. Tech stack

```
Backend:   Python 3.10 + Flask 3 + Flask-SocketIO
Database:  Portable layer (db.py) — works with SQLite locally or Postgres in production
DB driver: psycopg 3 (Postgres) / sqlite3 stdlib (local)
Frontend:  Server-rendered Jinja2 templates + Tailwind via CDN + small vanilla JS files
Real-time: Socket.IO over WebSocket (with polling fallback)
PDF:       reportlab (certificates)
AI:        urllib direct calls to Anthropic / OpenAI (no SDK dependency)
Email:     stdlib smtplib + ssl
Camera:    face-api.js loaded from CDN (used for proctoring)
Auth:      bcrypt-hashed passwords + Flask sessions
```

## 5. Project structure

```
QuizForge/
├── app.py                      # Main Flask app (all routes + Socket.IO handlers)
├── db.py                       # Portable DB layer (SQLite ↔ Postgres)
├── grading.py                  # QUESTION_TYPES list + grade_answer()
├── importers.py                # Bulk import (.docx, .csv, .txt)
├── exporters.py                # CSV / Excel export
├── ai_generator.py             # Anthropic / OpenAI integration
├── certificates.py             # reportlab PDF generation
├── email_send.py               # SMTP transport
├── requirements.txt
├── render.yaml                 # Render deployment config
├── Procfile
├── runtime.txt / .python-version
├── run.bat / run.sh            # Local launchers
├── .env.example
├── .gitignore
├── migrate_sqlite_to_postgres.py
├── check_neon.py               # Diagnostic: inspect Neon DB
├── cleanup_anonymous.py        # One-shot: delete all Anonymous attempts
├── cleanup_duplicate_answers.py
├── diagnose_polls_only.py      # Diagnostic per-quiz tallies
├── smoke_*.py                  # Smoke tests (live, autosave, dedupe, etc.)
├── docs/
│   ├── MARKET_ANALYSIS.md
│   ├── ROADMAP.md
│   ├── BUSINESS_PLAN.md
│   ├── FEATURE_INVENTORY.md
│   └── SESSION_HANDOFF.md      ← this doc
├── static/
│   ├── css/custom.css          # Shake/slide animations + a few overrides
│   ├── js/quiz-editor.js       # Admin quiz editor (loadless save)
│   ├── js/live-host.js         # Live session host panel
│   ├── js/live-student.js      # Live session student
│   └── uploads/                # File-upload field outputs (per quiz)
└── templates/
    ├── base.html
    ├── home.html               # SEO-rich landing page
    ├── pricing_page.html / features_page.html / use_cases_page.html
    ├── about_page.html / privacy_page.html / terms_page.html
    ├── login.html / register.html
    ├── forgot_password.html / reset_password.html
    ├── cert_verify.html        # Public certificate verification
    ├── admin/
    │   ├── dashboard.html      # New card-based dashboard (5 content kinds)
    │   ├── quiz_form.html      # Per-quiz editor with kind-aware sections
    │   ├── _question_card.html
    │   ├── quiz_results.html   # Exam results
    │   ├── poll_results.html   # Poll / survey / form results (charts + drill-downs)
    │   ├── attempt_detail.html # Per-attempt detail + violations + snapshots gallery
    │   ├── live_host.html      # Live session host UI
    │   ├── live_list.html      # All live sessions (clickable from dashboard)
    │   ├── question_bank.html  # Question bank CRUD + search
    │   ├── site_dashboard.html # Site admin stats
    │   ├── site_features.html  # Feature flags toggles
    │   ├── site_users.html     # User management
    │   └── import.html         # Bulk import UI fragments
    └── student/
        ├── quiz.html           # Massive single file — the student-facing quiz/form/poll runner
        ├── live.html           # Live session join page
        ├── results.html        # Per-attempt result page
        ├── join.html           # /j entry page
        ├── password_gate.html
        └── ip_blocked.html
```

## 6. Working conventions

- **Don't push to GitHub unless explicitly asked.** Pushing triggers a Render redeploy which historically wiped data, but since the Postgres migration, data persists. Still, push only on the user's instruction.
- **Always commit locally first** so the user can review.
- **Smoke-test every meaningful change** before pushing — there are several `smoke_*.py` scripts at the repo root that should keep passing.
- **Loadless UX**: the quiz editor doesn't page-reload on save/delete. Keep this pattern for new admin UIs.
- **Auto-save settings**: any setting in the quiz editor's sidebar that has `data-field="..."` auto-saves on change via `/admin/quizzes/<id>/setting`. Whitelist new fields in `_SETTING_TYPES` in app.py.
- **Feature flags first**: any new big feature should be gated by a flag in `FEATURE_DEFAULTS` (app.py) so it can be turned off site-wide.
- **Plan-gating**: AI is restricted to Pro+; `feature_enabled('feature_ai_quiz_gen')` AND `user_plan()['allow_ai']` must both be true.
- **Cache-busting**: static JS/CSS links use `?v={{ asset_v }}` so changes are picked up immediately.

## 7. Common commands

### Run locally
```cmd
cd C:\Users\Aflat\QuizForge
run.bat
```
Server at <http://localhost:5000>. Set `$env:DATABASE_URL = ""` to use local SQLite; set it to the Neon string to share data with production (be careful).

### Connect to production Postgres for a query
```powershell
$env:DATABASE_URL = "postgresql://neondb_owner:npg_C4yMZoKHlp5J@ep-mute-thunder-aqsz4egb.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require"
.\venv\Scripts\python.exe check_neon.py        # show user/quiz summary
.\venv\Scripts\python.exe cleanup_anonymous.py # dry-run
.\venv\Scripts\python.exe cleanup_anonymous.py --apply
```

### Push to production
```cmd
cd C:\Users\Aflat\QuizForge
git add <files>
git commit -m "..."
git push origin main
```
Render auto-deploys in ~2 minutes.

## 8. Features shipped (~180, grouped)

### Authentication & accounts
- Email + password sign-up (bcrypt) / sign-in / sign-out
- Forgot password with single-use 15-min tokens (via SMTP if configured, else flash)
- IP-level login rate limiting (8/15 min → HTTP 429)
- Account lockout after 10 failed attempts for 1 hour (HTTP 423)
- Last-login tracking, suspend / approve / promote, super-admin role
- Public sign-up gateable via Feature flags

### Plans & limits
- 4 plans: Free / Pro ($19) / Business ($49) / Enterprise (custom)
- Per-plan caps: quizzes, monthly attempts, AI access, white-label
- Quiz-creation blocked at quota with Upgrade link
- Live usage meter in dashboard header

### Feature flags (site-wide, super admin)
- `feature_registration`, `feature_ai_quiz_gen`, `feature_certificates`, `feature_live_mode`, `feature_polls`, `feature_anti_cheat`, `feature_exports`, `feature_billing`
- All toggle from `/admin/site/features` with auto-save

### Content kinds (5 prominent dashboard cards)
- **Exam / Quiz** — graded with pass mark + certificates
- **Poll** — opinion polls with bar charts + NPS + word cloud
- **Survey** — anonymous feedback
- **Form** — JotForm-style data collection (paginated by default)
- **Live Sessions** — clickable card to `/admin/live`

### Question / field types (~30)
**Classic**: mcq_single, mcq_multi, true_false, short_answer, long_answer, fill_blank, rating (1–5), nps (0–10), poll, open_ended, word_cloud

**Form fields (added later)**: email, phone, date, number, dropdown, url, time, datetime, slider, address (6 sub-fields), full_name, file_upload (async), signature (canvas), consent (terms), section_break (no answer)

**Interactive**: matching (pair via dropdown), ordering (up/down buttons), drag_drop (item→bin), hotspot (click on image)

### Editor productivity
- **Loadless** save/edit/delete (no page reloads)
- Auto-save all sidebar settings on change
- Bulk import: `.docx`, `.csv`, `.txt` (Aiken format)
- **AI Quiz Generator** (Anthropic / OpenAI) gated by API key + feature flag + Pro plan
- **Question Bank** (`/admin/bank`) with categories, tags, full-text search, "copy into quiz"
- Per-question time + bulk "apply N seconds to all"
- Per-question `Required` flag
- Image URL for hotspot questions

### Test administration & flow
- Whole-exam timer + auto-submit
- Per-question timer + auto-advance
- Paginated mode (one question per page, no back)
- Pass mark % + max attempts + randomize Qs/options (hash-seeded, no collisions)
- Schedule open / close (settings field — UI hint only at the moment)
- Forms default to paginated automatically

### Student experience
- Take via share link (no signup); join via 6-char code
- **Auto-save partial answers** every 0.8 seconds (resume on refresh)
- Browser-close warning if answered fields and not submitted
- **JotForm-style flow**: shake animation on required-empty, slide-in on advance
- Mobile-responsive everything
- Show correct answers after submit (configurable)
- Receives PDF certificate on pass with verification URL
- Real-time progress badge

### Anti-cheating (Tier 1 + 2 + AI camera)
- Tab/window switch detection (with violation log)
- Window blur detection
- Copy / paste / cut block
- Right-click block
- Text-selection block
- Force fullscreen + exit detection
- Dev-tools open detection
- Multi-tab detection (BroadcastChannel)
- IP allow-list (CIDR supported)
- Quiz password gate
- Auto-submit after N violations
- **AI camera proctoring** (face-api.js, hijab-friendly, snapshot evidence gallery)

### Results & analytics
- Score / max / percentage per attempt
- Pass/fail badge
- **Real-time partial scoring** (in-progress attempts show their current score)
- Per-question bar charts (MCQ / Yes-No / Poll)
- Rating distribution histogram + average
- NPS dashboard (Detractors / Passives / Promoters + score)
- Word cloud for open-text (frequency-weighted)
- **"Who picked this option"** drill-down (names per choice)
- Questions-answered count per respondent (X/N)
- Auto-refresh every 30s
- Per-respondent View / Delete buttons
- **🧹 Remove duplicates** + **🗑 Remove anonymous** admin buttons
- Auto-dedupe display (latest submission per name+email shown)
- Block duplicate submissions at submit time
- Manual grading for essays with feedback

### Exports
- CSV with per-question column + "Answered N/total" + violations count
- Excel (.xlsx) — same shape

### Certificates
- Auto-issued PDF (reportlab) on exam pass
- Unique serial (`XXXX-XXXX-XXXX`)
- **Public verification URL** `/verify/<serial>`
- Branded template with primary color + logo placeholder
- Download from results page

### Live mode (Kahoot-style)
- 6-character join code
- Real-time host dashboard with bar charts + leaderboard
- Reveal answers + push next question
- Live participant list with scores
- Auto-persist attempts to DB for Results page

### Admin / site management
- Site admin role (first user auto)
- Site dashboard: 8 platform stats
- User management: approve / suspend / promote / change plan
- Feature flags (8 site-wide toggles)
- Audit-friendly violation log per attempt

### Architecture & ops
- **Portable DB** layer — same code on SQLite (dev) or Postgres (prod) via `DATABASE_URL`
- Auto-migration on startup (idempotent column adds + new tables)
- Production data persists (Neon Postgres) — survives redeploys
- SEO landing page + JSON-LD + sitemap.xml + robots.txt
- Loadless admin UX everywhere

## 9. Database schema (current)

Major tables: `users`, `quizzes`, `questions`, `attempts`, `answers`, `live_sessions`, `violations`, `password_resets`, `site_settings`, `question_bank`, `certificates`, `proctor_snapshots`.

Key columns in `quizzes` (all auto-migrated): `kind` (exam/poll/survey/form), `time_limit_seconds`, `pass_mark`, `max_attempts`, `randomize_questions`, `randomize_options`, `paginated`, `quiz_password`, `anti_paste`, `anti_rightclick`, `block_selection`, `require_fullscreen`, `detect_tab_switch`, `detect_devtools`, `ip_allowlist`, `violation_limit`, `camera_proctor`, `proctor_snapshot_interval`.

Key columns in `questions`: `type`, `text`, `options` (JSON), `correct_answers` (JSON), `points`, `position`, `explanation`, `time_limit_seconds`, `image_url`, `is_required`.

## 10. What's NOT yet built (roadmap)

In rough priority order for sellability:

1. **Stripe billing** — convert plans to real subscriptions (Stripe Checkout, customer portal, webhooks)
2. **Multi-tenant organizations** — invite teachers under an org, classes, rosters, org-level branding
3. **Google / Microsoft SSO** (OAuth)
4. **LTI 1.3 + SCORM export** — for Canvas / Moodle / Schoology / Blackboard integration
5. **Email verification** on sign-up
6. **2FA** (TOTP / authenticator apps)
7. **Webhooks + public API**
8. **Question-bank import/export** (currently only manual entry + bulk via quiz import)
9. **Item analysis** (difficulty index, discrimination, longitudinal tracking)
10. **Mobile PWA + offline mode**
11. **Multi-language UI** (i18n)
12. **Public quiz / form library** (network-effect feature)
13. **Lockdown browser app** (Electron) for high-stakes exams

## 11. Useful smoke tests (all in repo root)

- `smoke_live.py` — Live mode end-to-end (host + student via socket.io client)
- `smoke_autosave.py` — Auto-save + randomization + partial scoring
- `smoke_dedupe.py` — Duplicate detection + Pass/Fail badge
- `smoke_anticheat.py` — Password, tab-switch, paste, violations, auto-submit
- `smoke_polls.py` — Poll-specific results dashboard
- `smoke_poll_drilldown.py` — "Who picked this" + Questions-answered column
- `smoke_poll_dedupe.py` — 3-layer duplicate protection
- `smoke_poll_reconcile.py` — Top counts == per-question counts
- `smoke_delete_anon.py` — Delete + anonymous + same-name handling
- `smoke_certs.py` — Certificate generation + verification + PDF download
- `smoke_e2e_http.py` — Engine-agnostic full flow (works against either SQLite or Postgres)

Run any of them while the local server is up to verify nothing's broken.

## 12. How to brief a fresh Claude session

Paste the entire content of this file, then add your specific request. Example:

> *"Read the SESSION_HANDOFF.md above for full context on QuizForge.*
> *Today I want you to start with: implement Stripe Checkout for upgrading from Free → Pro plan, using a test mode key I'll provide."*

Claude will know everything: tech stack, file locations, conventions, what's already built, credentials needed.

---

## SESSION LOG (newest first)

### 2026-05-16 — Fix attempt_detail TypeError + settings cache (low-resource win)
User opened an attempt detail page and got the new friendly 500 — the traceback (visible because they're super-admin) pinpointed `templates/admin/attempt_detail.html:50`: `q.options[i] if i < q.options|length` blew up with `TypeError: '<' not supported between instances of 'str' and 'int'`. Root cause: some answers and `correct_answers` lists were stored as strings (`["0","1"]`) instead of ints, so the Python comparison crashed at template-render time.

**Server side (`app.py`):**
- New `_coerce_to_int()` / `_coerce_to_int_list()` helpers normalize messy DB data.
- `attempt_detail` route now coerces both `q.correct_answers` and the parsed `a.value` to ints for choice-question types before passing to the template.
- New `opt_label(options, idx)` helper registered as a Jinja global — does safe coercion + bounds check so templates can never crash on a weird index.
- New `int_list` Jinja filter for `{% if loop.index0 in q.correct_answers|int_list %}` — applied to 4 other templates (`student/results.html`, `admin/question_bank.html`, `admin/quiz_results.html`, `admin/_question_card.html`) where the same string-vs-int mismatch was silently marking correct answers as wrong.

**Low-resource efficiency (`app.py`):**
- Added a per-process in-memory cache for the site-settings table with a 30s TTL (configurable via `SETTINGS_CACHE_TTL` env var). Settings change rarely but are read on EVERY page render. With caching, a logged-in page-render no longer touches the DB for feature flags **at all** after the first hit. `_settings_set` invalidates the cache so flag toggles are visible immediately.
- On Render free tier (Neon ~1s cold latency, ~50ms warm) this saves the DB roundtrip on every navbar render → roughly **2× faster perceived navigation** for logged-in admins.

**Smoke test (`smoke_attempt_detail.py`):** 25 checks covering attempt_detail rendering with legacy string-index data, all opt_label edge cases (string/int/None/out-of-range/empty), coerce helpers, int_list Jinja filter, cache-hit / cache-miss behavior, and `_settings_set` cache invalidation.

### 2026-05-16 — Submit feedback + 6× faster submit (no more double-submits)
User reported: "when student clicks Submit, it takes time and they think it's broken so they click 3-4 times". Fixed both sides:

**Client side (`templates/student/quiz.html`):**
- New full-screen `#qf-submitting-overlay` with spinner + "Submitting your answers…" message — shows the **instant** the form starts submitting (well before the server roundtrip completes).
- `showSubmittingOverlay()` helper that (a) reveals the overlay, (b) disables EVERY `<button>` and `<input type=submit>` on the page, (c) sets `aria-busy=true`, (d) renames the Submit label to "Submitting…". A second click is now physically impossible.
- Called from all 4 submit paths: the natural `submit` event, the paginated last-page `advance()` (because `form.submit()` does NOT fire the submit event!), the timer auto-submit, and the anti-cheat violation auto-submit.
- Belt-and-suspenders `if (isSubmitting) e.preventDefault()` in the submit handler so even an extension that re-enables the button can't fire a duplicate POST.

**Server side (`app.py`):**
- The submit handler used to do **one INSERT per question** — on Neon that's N network roundtrips (~50ms each), so a 20-question quiz took >1s just on the INSERT loop. New `db.executemany()` helper does the whole batch in a single roundtrip. Combined with the connection retry from the previous fix, real-world submits go from 1-2s perceived to <300ms.
- New `db.Connection.executemany()` works on both Postgres (psycopg's native executemany) and SQLite.

**Smoke test (`smoke_submit_speed.py`):** 14 checks including overlay markup present, helper wired into all 4 submit paths, server uses `conn.executemany`, real submit produces correct score via batched insert, double-submit doesn't crash the server.

### 2026-05-16 — Fix "Internal Server Error" on admin pages (root cause + safety net)
User reported "most admin pages" were intermittently returning Flask's generic 500. Root cause was a **DB connection storm**: every page render fired `features_all()`, which opened **8 separate `psycopg.connect()` calls** (one per feature flag) plus `current_user()` + `user_plan()` + `user_usage()` — ~12 fresh Neon connections per admin page load. On Neon free tier with cold-start latency, this was guaranteed to randomly fail.

Three-layer fix:
1. **`features_all()` rewritten** — new `_settings_get_many()` does one `SELECT ... WHERE key IN (?, ?, ...)` query. Connections per page render drop from ~12 to ~5. Smoke test confirms: 8 connections → 1.
2. **`db.get_conn()` hardened** — added `connect_timeout=10s` and a 3-try retry loop with exponential backoff (0.4s, 0.8s, 1.6s) for transient Neon disconnects. Configurable via `PG_CONNECT_TIMEOUT` / `PG_CONNECT_RETRIES` env vars.
3. **Global 500 handler** in `app.py` — every unhandled exception now (a) logs full traceback to stderr with a unique `err_xxxxxxxx` ID (Render captures stderr), (b) renders a friendly `templates/error_500.html` page with the ID, and (c) shows the traceback inline to super-admins only (regular users get the generic message). HTTPExceptions (404, 401, abort) still pass through unchanged. Made `inject_globals()` defensive so a DB hiccup mid-render doesn't recurse.

Now when an admin page DOES error, you'll see the error ID on screen + the full traceback in Render logs — no more guessing. Smoke test covers 17 cases (admin dashboard, site features, site users, live list, bank, crash route 500-handling for super and non-super, 404 passthrough, single-connection features_all, retry config).

### 2026-05-16 — Always-on AI overlay + Snapshots column on Results
User reported "i cannot see any tracking lines" and asked where snapshots are saved per user. Two fixes:
1. **Visual overlay made always-on**: Restructured into two parallel loops — a *visual loop* @ 100ms that always draws pulsing corner brackets + last-detection landmarks (so something is visible even when face-api is loading or has missed a frame), and a *detection loop* @ 600ms (setup) / 2000ms (exam) that runs the actual face-api inference. Thicker 3px box stroke, 2.4px landmark dots, `shadowBlur=8` glow effect. More polylines drawn: jaw, both brows, nose bridge, nose base, both eyes, outer + inner lips. Added a diagnostic banner showing AI tracker status ("loading models" → "tracking face"). Added unpkg CDN fallback when jsdelivr fails.
2. **Snapshot discoverability**: Added a `📷` column on `templates/admin/quiz_results.html` showing snapshot count per attempt — clicking it deep-links to the attempt detail page's `#snapshots` anchor. The gallery on the detail page is now `<details id="snapshots" open>` so it expands by default when linked. `quiz_results` route in `app.py` builds a `snap_map` dict per attempt for the count badges.

### 2026-05-16 — Live AI overlay + remove aligned-gate
The proctor gate now draws a live AI tracker overlay on the camera feed — bounding box + 68 facial landmarks + eye polygons + lip outline. Updates every 700ms during setup and every 1 second during the exam, so the student can see the AI actually tracking them in real time. Color codes: green = looking straight, amber = face turned, red = no face / multiple faces. Also removed the brittle `aligned` flag from the Continue click handler — now the only gate is the button's `disabled` attribute, so any path that enables the button (3-stable-detections, skip-AI link, 15s fallback, model-not-loaded path) allows clicking. Same overlay added to the mini preview during the exam.

### 2026-05-16 — Camera v2 click-handler bug fix
The 15-second fallback was enabling the green Continue button visually but the click handler `if (!aligned) return;` silently bailed because `aligned` stayed false. Now every "let the student proceed" path (model-loaded path, model-skipped path, 15-second timeout) sets `aligned = true` before unlocking the button. Verified: clicking the button now actually transitions into the exam.

### 2026-05-16 — face-api loader fix
The library/models URLs in v2 were mismatched (original face-api.js JS but vladmandic model paths). Pinned to `@vladmandic/face-api@1.7.13` for both library AND model weights, switched to `.load()` from `.loadFromUri()`. Added console.error logging and a visible "Continue without AI detection" fallback button if the model can't download. Hard 15-second fallback enables Continue even if everything fails so a student can't be locked out by a CDN hiccup.

### 2026-05-16 — Camera proctoring v2 (full-page gate, face-angle detection)
Replaced the inline consent banner with a **full-page modal gate** that blocks the entire quiz until camera is enabled AND a face is stably detected for 3 consecutive seconds. Setup instructions on the gate (lighting / oval alignment / no head tilt). Loads `tinyFaceDetector` + `faceLandmark68Net` from CDN. New `estimateYaw()` computes head-turn from nose vs face-center offset; >25° for 8s logs a `looking_away` violation with snapshot. Always captures baseline snapshot at start regardless of model load state, so admin gallery always populated. Mini preview pinned bottom-right during exam with live status pill.

### 2026-05-16 — Demo content + JSON import template
One-click demo content creators on the dashboard: **📝 Demo Form** / **📋 Demo Exam** / **📊 Demo Poll** — each builds a fully-populated quiz with every supported field type as example questions. **⬇ Download JSON template** link gets the canonical bulk-import format. Bulk-import section now also accepts **.json** files / paste — supports every field type including the complex ones (matching/ordering/drag-drop/hotspot/address/signature/etc.). `demo_content.py` module holds all three templates. ~185 features.

### 2026-05-16 — Handoff document created
Saved this doc. ~180 features total.

### 2026-05-16 — 10 more form-field types
Added: url, time, datetime, slider, address (6 sub-fields), full_name, file_upload (async upload endpoint), signature (canvas with touch support), consent (terms checkbox), section_break (no answer). Server endpoint `/q/<code>/upload` for file uploads. All work with the JotForm-style required-validation flow.

### 2026-05-16 — JotForm-style paginated flow
Per-question `is_required` flag with editor checkbox. Forms now default to paginated. CSS shake animation on validation fail with "This field is required" pill. Smooth slide-in transition between questions. Auto-focus first input on advance. Forms default to paginated mode automatically.

### 2026-05-16 — 4 interactive question types
Matching (pair via dropdown), Ordering (up/down buttons), Drag-Drop (item→bin), Image Hotspot (click on image with normalized coordinates). Added `image_url` column for hotspots. All gradeable with partial credit where appropriate.

### 2026-05-15 — Forms + new field types + Live as clickable card
Added `kind='form'` as a new content type (JotForm-style). 5 base field types: email, phone, date, number, dropdown. Dashboard now 5 prominent cards: Exams · Polls · Surveys · Forms · Live Sessions (clickable). New page `/admin/live` lists every live session ever hosted.

### 2026-05-15 — AI Camera Proctoring (Tier 3 anti-cheating)
face-api.js loaded from CDN. Per-quiz toggle for webcam-required exams. Hijab-friendly (face region only, no hair detection, lenient threshold). Auto-capture snapshot evidence (configurable interval + on violations). Admin gallery on per-attempt detail page. Consent banner before camera starts.

### 2026-05-15 — Question Bank + AI Generator + SMTP
Question bank at `/admin/bank` with CRUD, categories, tags, full-text search, "copy into quiz". AI quiz generation via Anthropic (`claude-3-5-sonnet-latest`) or OpenAI (`gpt-4o-mini`); plan-gated to Pro+ and feature-flagged. SMTP email transport for password resets (graceful fallback to flash when SMTP not configured).

### 2026-05-15 — Plans + PDF Certificates + Dashboard redesign
4 plans: Free/Pro/Business/Enterprise with usage caps. Super admins get Enterprise. Plan badge + usage meter in dashboard header. Quiz-creation blocked at quota. PDF certificates auto-issued on pass via reportlab. Verification URL `/verify/<serial>`. Dashboard redesigned to 4 (now 5) prominent content-kind cards.

### 2026-05-15 — Loadless editor + Site Feature Flags
Quiz editor no longer reloads on save/edit/delete. 8 site-wide feature flag toggles (AI, certificates, live, polls, etc.) at `/admin/site/features` with auto-save. Cache-busted static assets.

### 2026-05-14 — Sign-in security + Tier-2 anti-cheating + FEATURE_INVENTORY
Forgot-password flow with 15-min single-use tokens. IP rate limiting (8/15min → 429). Account lockout (10 fails → 423 for 1h). IP allow-list per quiz (CIDR support). Dev-tools open detection. Multi-tab detection via BroadcastChannel. Comprehensive `docs/FEATURE_INVENTORY.md` with all ~190 competitor features mapped.

### 2026-05-14 — Postgres migration complete
Portable `db.py` layer that auto-translates SQLite SQL ↔ Postgres at runtime. Neon Postgres free tier wired in via `DATABASE_URL`. Data now survives every redeploy. Migration script for SQLite→Postgres data export.

### 2026-05-13 → 14 — Polls v2: dedupe + drill-down + anonymous cleanup + admin enhancements
Per-poll dedupe (latest per name+email), "Who picked this" drill-down, per-respondent View/Delete buttons, 🧹 Remove duplicates + 🗑 Remove anonymous bulk-action buttons, Questions Answered count column, real-time partial scoring on results page, top stat reconciliation.

### 2026-05-13 — Polls v1: NPS + word cloud + visual dashboard
New `admin/poll_results.html` with bar charts, NPS widget, word cloud, copy-link button, auto-refresh. NPS (0–10) added as a question type.

### 2026-05-12 → 13 — Tier 1 Anti-cheating + Auto-save + Randomization
Tab switch, paste, right-click, fullscreen, password gate, auto-submit on violations. Auto-save partial answers every 0.8s. Hash-seeded randomization (no small-list collisions). Browser-close warning. Per-question time + paginated mode.

### 2026-05-12 — Super-admin + Live attempts in DB + Bug fixes
First user becomes super-admin. Site admin dashboard + user management. Live mode session_id bug fixed (was dropping all answers). Live attempts persisted to DB with "Live" badge in Results. Various smoke tests added.

### 2026-05-11 → 12 — Initial build
Phase 0: Flask + SQLite + auth + 10 question types + bulk import + CSV/Excel export + live mode (Socket.IO) + manual grading + market analysis doc + 90-day roadmap doc. Deployed to Render free tier. GitHub repo created (HanbalIshaq/quizforge).
